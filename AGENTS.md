# AGENTS.md

## Git workflow
- After completing changes, review them myself (staged/unstaged diffs) before committing.
- If the changes look correct, commit them with a clear conventional-style message and push to `origin` (GitHub) automatically — do NOT wait for the user to ask.
- Only skip commit/push if something looks wrong or the user explicitly says not to.
